<?php

// autoload.php @generated by Yasin Timur
error_reporting(1);
require_once __DIR__ . '/../src/Vodafone.php';

$requestFile = scandir(__DIR__ . "/../src/Request");

foreach ($requestFile as $file) {

    if (is_dir($file)) continue;

    require_once __DIR__ . "/../src/Request/" . $file;
}
$servicesFile = scandir(__DIR__ . "/../src/Services");

foreach ($servicesFile as $file) {
    if (is_dir($file)) continue;
    require_once __DIR__ . "/../src/Services/" . $file;
}
